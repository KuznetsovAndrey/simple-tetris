{
  "name": "tetris",
  "version": "0.0.1",
  "dependencies": {
    "express": ">=4.10.2",
    "forever": ">=0.13.0"
  },
  "scripts": {
    "start": "./node_modules/forever/bin/forever --watchDirectory ./ --watch --verbose tetris.js local"
  }
}
